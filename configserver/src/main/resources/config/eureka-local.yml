eureka:
  instance:
    hostname: ivfzhoudebian
    prefer-ip-address: true # 使用 IP 注册（否则使用主机名）。
    instance-id: ${spring.application.name}:${server.port}
  client:
    service-url:
      defaultZone: http://ivfzhoudebian:8761/eureka
