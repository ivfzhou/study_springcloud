server:
  port: 8761
spring:
  application:
    name: eureka-server
eureka:
  client:
    register-with-eureka: false # 自身不注册到自己。
    fetch-registry: false # 不拉取服务注册信息。
  server:
    # enable-self-preservation: false # 关闭自我保护（开发环境常用）。
