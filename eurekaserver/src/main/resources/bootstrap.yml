# Spring Cloud Config 配置中心引导文件
spring:
  cloud:
    config:
      # 配置中心服务地址
      uri: http://ivfzhoudebian:8080
      # 加载的配置文件名称（Eureka服务端专属配置）
      name: eureka_server
      # 激活的环境配置
      profile: local
