# Spring Cloud Config 配置中心引导文件
spring:
  cloud:
    config:
      # 配置中心服务地址
      uri: http://ivfzhoudebian:8080
      # 加载的配置文件名称（eureka、日志、rabbitmq、redis、mysql、公共配置、搜索服务配置）
      name: eureka,log4j2,rabbitmq,redis,mysql,common,search
      # 激活的环境配置
      profile: local
